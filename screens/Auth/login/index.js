import {View,Text,TextInput,SafeAreaView,TouchableOpacity,Platform,Dimensions,TouchableWithoutFeedback,Keyboard,KeyboardAvoidingView} from "react-native"

export default function Login(){
    return(
        <SafeAreaView>
            
        </SafeAreaView>
    )
}